#include<iostream>
#include "Router.cpp"
#include "CentralRouter.cpp"
using namespace std;

int main()
{  
    /*int n;       //n == number of all nodes
    int r = 2;   //number of normal routers is 2 only

    Node nodes[n];                          //instantiate n number of nodes
    Router Routers[2];                      // only 2 routers
    Central_router central_router;          //one central_router
    
    //for central router initialisation
    cin>>central_router.mac_address>>central_router.ip_address>>central_router.queue_length;

    for(int i=0;i<2;i++)
    {
       cin>>Routers[i].mac_address>>Routers[i].ip_address>>Routers[i].queue_length;
    }

    for(int i=0;i<n;i++)
    {
    	cin>>node[i].mac_address>>node[i].ip_address>>node[i].router_address;
    	// so the values are given in here
    }

    //assigned addresses and initialisation done for all the nodes
    //now moving onto packets stuff
    //hard code the packet initialisation here....*/
    
    return 0;
}